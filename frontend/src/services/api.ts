/**
 * Servico de comunicacao com a API REST (FastAPI).
 * Sera implementado nos proximos PRs.
 */

const API_BASE_URL = import.meta.env.VITE_API_URL || "http://localhost:8000";

export default API_BASE_URL;
